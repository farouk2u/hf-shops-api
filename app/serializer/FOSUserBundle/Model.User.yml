FOS\UserBundle\Model\User:
    exclusion_policy: ALL
    properties :
        id:
            expose: true

        email:
            expose: true

        likedShops:
            expose: true

        likedShopsIds:
             expose: true

        disLikedShops:
            expose: true

        disLikedShopsIds:
             expose: true

        lastLogin:
            expose: true